export class Upload {
    constructor () {

    }

    async execute () {
        
    }

    async editResponse () {
        
    }
}

const delay = (ms) => new Promise(resolve => setTimeout(resolve, ms));